name: Build & Deploy

on:
  push:
    branches:
      - main
      - master
  pull_request:
  schedule:
    # 🌟 完整构建 - 每天 2 次 (05:00, 17:00 UTC)
    # 执行: 镜像同步 + 插件转换 + 模块合并 + 规则构建 + 部署
    - cron: '0 5,17 * * *'

    # ⚡ 快速更新 - 每 4 小时 (00:00, 04:00, 08:00, 12:00, 16:00, 20:00 UTC)
    # 执行: 规则构建 + 部署 (智能增量,不覆盖其他目录)
    - cron: '0 */4 * * *'

    # 🪞 镜像同步 - 每天 3 次 (06:00, 14:00, 22:00 UTC)
    # 执行: 镜像同步 + 规则构建 + 部署 (智能增量)
    - cron: '0 6,14,22 * * *'

    # 🔌 插件转换 - 每天 2 次 (07:30, 19:30 UTC)
    # 执行: 插件转换 + 模块合并 + 规则构建 + 部署 (智能增量)
    - cron: '30 7,19 * * *'
  workflow_dispatch:
    inputs:
      task:
        description: '执行任务 (all/build/convert-plugins/merge-modules/mirror-sync/deploy)'
        required: false
        default: 'all'
        type: choice
        options:
          - all
          - build
          - convert-plugins
          - merge-modules
          - mirror-sync
          - deploy
      deploy_target:
        description: '部署目标 (all/github/cloudflare)'
        required: false
        default: 'all'
        type: choice
        options:
          - all
          - github
          - cloudflare

concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  # ============================================
  # 准备阶段：确定需要执行的任务
  # ============================================
  prepare:
    name: Prepare Tasks
    runs-on: ubuntu-latest
    outputs:
      should_build: ${{ steps.decide.outputs.should_build }}
      should_convert_plugins: ${{ steps.decide.outputs.should_convert_plugins }}
      should_merge_modules: ${{ steps.decide.outputs.should_merge_modules }}
      should_mirror_sync: ${{ steps.decide.outputs.should_mirror_sync }}
      should_deploy: ${{ steps.decide.outputs.should_deploy }}
      deploy_target: ${{ steps.decide.outputs.deploy_target }}
    steps:
      - name: Decide tasks to run
        id: decide
        run: |
          # 🔧 修复: 正确处理空字符串的情况
          TASK="${{ github.event.inputs.task }}"
          if [ -z "$TASK" ]; then
            TASK="all"
          fi

          IS_SCHEDULE="${{ github.event_name == 'schedule' }}"
          IS_PUSH="${{ github.event_name == 'push' }}"
          IS_PR="${{ github.event_name == 'pull_request' }}"
          IS_WORKFLOW_DISPATCH="${{ github.event_name == 'workflow_dispatch' }}"
          CRON="${{ github.event.schedule }}"

          # 默认值
          BUILD="false"
          CONVERT_PLUGINS="false"
          MERGE_MODULES="false"
          MIRROR_SYNC="false"
          DEPLOY="false"

          DEPLOY_TARGET="${{ github.event.inputs.deploy_target }}"
          if [ -z "$DEPLOY_TARGET" ]; then
            DEPLOY_TARGET="all"
          fi

          # 手动触发 - 根据 task 参数决定
          if [ "$IS_WORKFLOW_DISPATCH" = "true" ]; then
            if [ "$TASK" = "all" ] || [ "$TASK" = "build" ]; then BUILD="true"; fi
            if [ "$TASK" = "all" ] || [ "$TASK" = "convert-plugins" ]; then CONVERT_PLUGINS="true"; fi
            if [ "$TASK" = "all" ] || [ "$TASK" = "merge-modules" ]; then MERGE_MODULES="true"; fi
            if [ "$TASK" = "all" ] || [ "$TASK" = "mirror-sync" ]; then MIRROR_SYNC="true"; fi
            if [ "$TASK" = "all" ] || [ "$TASK" = "deploy" ]; then DEPLOY="true"; fi
          fi

          # Push 触发 - 执行完整流程（优先级最高）
          if [ "$IS_PUSH" = "true" ]; then
            CONVERT_PLUGINS="true"
            MERGE_MODULES="true"
            MIRROR_SYNC="true"
            BUILD="true"
            DEPLOY="true"
          fi

          # 定时任务（仅在非 push 时生效）
          if [ "$IS_SCHEDULE" = "true" ] && [ "$IS_PUSH" != "true" ]; then
            case "$CRON" in
              "0 5,17 * * *")
                # 🌟 完整构建 - 执行所有任务 (每天 2 次)
                MIRROR_SYNC="true"
                CONVERT_PLUGINS="true"
                MERGE_MODULES="true"
                BUILD="true"
                DEPLOY="true"
                ;;
              "0 */4 * * *")
                # ⚡ 快速更新 - 仅规则构建 (每 4 小时)
                BUILD="true"
                DEPLOY="true"
                ;;
              "0 6,14,22 * * *")
                # 🪞 镜像同步 - 镜像 + 规则 (每天 3 次)
                MIRROR_SYNC="true"
                BUILD="true"
                DEPLOY="true"
                ;;
              "30 7,19 * * *")
                # 🔌 插件转换 - 插件 + 模块 + 规则 (每天 2 次)
                CONVERT_PLUGINS="true"
                MERGE_MODULES="true"
                BUILD="true"
                DEPLOY="true"
                ;;
            esac
          fi

          # PR 触发
          if [ "$IS_PR" = "true" ]; then
            BUILD="true"
            DEPLOY="false"
          fi

          echo "should_build=$BUILD" >> $GITHUB_OUTPUT
          echo "should_convert_plugins=$CONVERT_PLUGINS" >> $GITHUB_OUTPUT
          echo "should_merge_modules=$MERGE_MODULES" >> $GITHUB_OUTPUT
          echo "should_mirror_sync=$MIRROR_SYNC" >> $GITHUB_OUTPUT
          echo "should_deploy=$DEPLOY" >> $GITHUB_OUTPUT
          echo "deploy_target=$DEPLOY_TARGET" >> $GITHUB_OUTPUT

          echo "📋 Trigger Information:"
          echo "  Event: ${{ github.event_name }}"
          echo "  Task: $TASK"
          echo "  Is Schedule: $IS_SCHEDULE"
          echo "  Is Push: $IS_PUSH"
          echo "  Is PR: $IS_PR"
          echo "  Is Workflow Dispatch: $IS_WORKFLOW_DISPATCH"
          echo "  Cron: $CRON"
          echo ""
          echo "📋 Tasks to run:"
          echo "  Build: $BUILD"
          echo "  Convert Plugins: $CONVERT_PLUGINS"
          echo "  Merge Modules: $MERGE_MODULES"
          echo "  Mirror Sync: $MIRROR_SYNC"
          echo "  Deploy: $DEPLOY (target: $DEPLOY_TARGET)"

  # ============================================
  # 插件转换任务 - 只有需要插件转换时才启动 Script-Hub
  # ============================================
  convert-plugins:
    name: Convert Plugins
    needs: prepare
    if: needs.prepare.outputs.should_convert_plugins == 'true'
    runs-on: ubuntu-latest
    services:
      script-hub:
        # Docker Hub latest digest retrieved 2026-07-24 (multi-arch, including ARM64).
        image: xream/script-hub@sha256:c55180dd41c07567906f17953587c25b61b427b2c5cc6b955721677fd615f470
        ports:
          - 9100:9100
          - 9101:9101
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
          fetch-depth: 1

      - uses: pnpm/action-setup@0977fd99725f1db4007ccb2928dbb4e90d06cc86 # v6.0.10

      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version-file: ".node-version"
          cache: "pnpm"

      - run: pnpm install --frozen-lockfile

      - name: Setup plugin output directories
        run: mkdir -p public/Modules/Converted public/Scripts

      - name: Configure Script-Hub
        run: |
          echo "127.0.0.1 script.hub" | sudo tee -a /etc/hosts
          echo "⏳ 等待 Script-Hub 服务启动..."
          for i in {1..30}; do
            if curl -f -s http://script.hub:9101/ > /dev/null 2>&1; then
              echo "✓ Script-Hub 服务就绪 (尝试 $i/30)"
              sleep 10
              exit 0
            fi
            echo "  尝试 $i/30 失败，等待 5 秒后重试..."
            sleep 5
          done
          echo "❌ Script-Hub 服务启动超时"
          exit 1

      - name: Convert plugins
        continue-on-error: true
        run: |
          pnpm run convert-plugins --wait-service --timeout 600 || {
            echo "⚠️ 首次转换失败，等待 10 秒后重试..."
            sleep 10
            pnpm run convert-plugins --wait-service --timeout 600 || {
              echo "❌ 两次转换都失败，继续构建流程"
            }
          }
        env:
          CI: true
          # Maintainer-controlled Worker fallback; provenance and SHA-256 warnings flag content changes.
          PROXY_BASE: https://cloudflare-proxy.lucking.workers.dev

      - name: Prepare plugin artifact marker
        if: always()
        run: |
          mkdir -p public/_artifacts
          printf 'plugin-output-ready\n' > public/_artifacts/plugin-output-ready

      - name: Upload plugin conversion output
        if: always()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: plugin-output-${{ github.sha }}-${{ github.run_number }}
          path: |
            public/_artifacts
            public/Modules/Converted
            public/Scripts
          retention-days: 1
          compression-level: 4
          if-no-files-found: warn

  # ============================================
  # 模块合并任务 - 不需要 Script-Hub 服务
  # ============================================
  merge-modules:
    name: Merge Modules
    needs: [prepare, convert-plugins]
    if: |
      always() &&
      needs.prepare.outputs.should_merge_modules == 'true' &&
      (needs.convert-plugins.result == 'success' || needs.convert-plugins.result == 'skipped')
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
          fetch-depth: 1

      - uses: pnpm/action-setup@0977fd99725f1db4007ccb2928dbb4e90d06cc86 # v6.0.10

      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version-file: ".node-version"
          cache: "pnpm"

      - run: pnpm install --frozen-lockfile

      - name: Setup module output directories
        run: mkdir -p public/Modules/Converted public/Modules/Merged public/Modules/Rules public/Scripts

      - name: Download plugin conversion output
        if: needs.prepare.outputs.should_convert_plugins == 'true'
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: plugin-output-${{ github.sha }}-${{ github.run_number }}
          path: public

      - name: Ensure converted modules exist
        run: |
          CONVERTED_DIR="public/Modules/Converted"
          if [ -d "$CONVERTED_DIR" ]; then
            FILE_COUNT=$(find "$CONVERTED_DIR" -type f -name '*.sgmodule' 2>/dev/null | wc -l)
          else
            FILE_COUNT=0
          fi

          if [ "$FILE_COUNT" -gt 0 ]; then
            echo "✅ 已有 $FILE_COUNT 个转换后的模块文件，无需下载"
            exit 0
          fi

          echo "📥 转换后的模块文件缺失，从生产环境下载..."
          git clone --depth=1 --filter=blob:none --sparse \
            https://github.com/lucking7/NRRule.git ./temp-converted 2>/dev/null || {
            echo "⚠️ 无法下载生产环境的模块文件，模块合并可能失败"
            exit 0
          }
          cd ./temp-converted
          git sparse-checkout set Modules/Converted
          cd ..

          mkdir -p "$CONVERTED_DIR"
          if [ -d "temp-converted/Modules/Converted" ]; then
            cp -r temp-converted/Modules/Converted/* "$CONVERTED_DIR/" 2>/dev/null || true
            echo "✅ 从生产环境下载了 $(find "$CONVERTED_DIR" -type f -name '*.sgmodule' 2>/dev/null | wc -l) 个模块文件"
          fi
          rm -rf ./temp-converted

      - name: Merge modules
        run: |
          mkdir -p public/Modules/Rules
          pnpm run node ./Build/merge-modules.ts --config Build/lib/module-merger/configs/pro-merge-config.yaml
        env:
          CI: true

      - name: Upload module output
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: module-output-${{ github.sha }}-${{ github.run_number }}
          path: |
            public/Modules
            public/Scripts
          retention-days: 1
          compression-level: 4
          if-no-files-found: warn

  # ============================================
  # 核心构建任务 - 包含规则生成与最终产物
  # ============================================
  build:
    name: Build
    needs: [prepare, convert-plugins, merge-modules]
    if: |
      always() &&
      needs.prepare.outputs.should_build == 'true' &&
      (needs.convert-plugins.result == 'success' || needs.convert-plugins.result == 'skipped') &&
      (needs.merge-modules.result == 'success' || needs.merge-modules.result == 'skipped')
    runs-on: ubuntu-latest
    steps:
      # 🚀 网络性能优化
      - uses: smorimoto/tune-github-hosted-runner-network@bb252dcb5c8609a31087e7993daa086f5a1c0069 # v1.0.0

      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
          fetch-depth: 1

      - uses: pnpm/action-setup@0977fd99725f1db4007ccb2928dbb4e90d06cc86 # v6.0.10

      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version-file: ".node-version"
          cache: "pnpm"

      - name: Setup build directory
        id: build-dir
        run: |
          echo "dir=public" >> $GITHUB_OUTPUT
          mkdir -p public

      - name: Get current date
        id: date
        run: |
          echo "date=$(date +'%Y-%m-%d %H:%M:%S')" >> $GITHUB_OUTPUT
          echo "year=$(date +'%Y')" >> $GITHUB_OUTPUT
          echo "month=$(date +'%m')" >> $GITHUB_OUTPUT
          echo "day=$(date +'%d')" >> $GITHUB_OUTPUT

      # 缓存策略：
      # - 主键包含 run_id，确保每次运行有唯一缓存，避免并发冲突
      # - restore-keys 按时间粒度降级：同日 > 同月 > 同年 > 任意
      # - 这样既避免了 "another job may be creating this cache" 错误
      # - 又能最大化利用最近的缓存数据
      - name: Restore cache
        uses: actions/cache/restore@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: .cache
          key: ${{ runner.os }}-build-v3-${{ steps.date.outputs.year }}-${{ steps.date.outputs.month }}-${{ steps.date.outputs.day }}-${{ github.run_id }}
          restore-keys: |
            ${{ runner.os }}-build-v3-${{ steps.date.outputs.year }}-${{ steps.date.outputs.month }}-${{ steps.date.outputs.day }}-
            ${{ runner.os }}-build-v3-${{ steps.date.outputs.year }}-${{ steps.date.outputs.month }}-
            ${{ runner.os }}-build-v3-${{ steps.date.outputs.year }}-
            ${{ runner.os }}-build-v3-

      - run: pnpm install --frozen-lockfile

      - name: Validate code quality
        run: pnpm run validate

      - name: Run regression tests
        run: pnpm test

      - name: Check unused code and dependencies
        run: pnpm run knip

      # 🔧 步骤 1: 镜像同步（如果需要）
      - name: Sync mirrors (iRingo, DualSubs, BiliUniverse)
        if: needs.prepare.outputs.should_mirror_sync == 'true'
        run: pnpm run node ./Build/sync-mirrors.ts
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          CI: true

      - name: Download Sukka mock & sgmodule
        if: needs.prepare.outputs.should_mirror_sync == 'true'
        run: pnpm run node ./Build/download-mock-modules.ts
        env:
          CI: true

      - name: Download fmz200 Split Modules
        if: needs.prepare.outputs.should_mirror_sync == 'true'
        run: pnpm run node ./Build/download-fmz200-split.ts
        env:
          CI: true

      - name: Download plugin conversion output
        if: |
          needs.prepare.outputs.should_convert_plugins == 'true' &&
          needs.prepare.outputs.should_merge_modules != 'true' &&
          needs.convert-plugins.result == 'success'
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: plugin-output-${{ github.sha }}-${{ github.run_number }}
          path: public

      - name: Download module output
        if: |
          needs.prepare.outputs.should_merge_modules == 'true' &&
          needs.merge-modules.result == 'success'
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: module-output-${{ github.sha }}-${{ github.run_number }}
          path: public

      # 🔧 步骤 4.2: 清理 Modules 根目录中的历史 .sgmodule 文件
      - name: Clean legacy Modules root files
        if: needs.prepare.outputs.should_build == 'true'
        run: |
          echo "🧹 清理 public/Modules 根目录中的历史 .sgmodule 文件..."
          if [ -d "public/Modules" ]; then
            LEGACY_COUNT=$(find public/Modules -maxdepth 1 -type f -name '*.sgmodule' 2>/dev/null | wc -l)
            if [ "$LEGACY_COUNT" -gt 0 ]; then
              echo "  🗑️  将删除 $LEGACY_COUNT 个旧的根目录 .sgmodule 文件"
              find public/Modules -maxdepth 1 -type f -name '*.sgmodule' -print -delete
            else
              echo "  ✅ 没有需要清理的根目录 .sgmodule 文件"
            fi
          fi
          # 确保新目录结构存在
          mkdir -p public/Modules/Converted public/Modules/Merged public/Modules/Rules

      # 🔧 步骤 4.5: 下载缺失的目录用于生成完整的 index.html
      - name: Download missing directories for index.html
        if: needs.prepare.outputs.should_build == 'true'
        run: |
          echo "📥 检查并下载缺失的目录..."
          echo "📁 当前 public 目录内容："
          ls -la public/ || echo "public 目录不存在"

          # 检查哪些目录缺失或为空（包含所有可能的规则目录）
          # 🔧 修复：不仅检查目录是否存在，还检查是否有内容
          MISSING_DIRS=""
          for dir in Mirror Modules Scripts List Clash sing-box Loon GeoIP; do
            if [ ! -d "public/$dir" ]; then
              MISSING_DIRS="$MISSING_DIRS $dir"
              echo "  ❌ $dir/ 缺失"
            else
              FILE_COUNT=$(find "public/$dir" -type f 2>/dev/null | wc -l)
              if [ "$FILE_COUNT" -eq 0 ]; then
                # 🔧 目录存在但为空，也需要从生产环境下载
                MISSING_DIRS="$MISSING_DIRS $dir"
                echo "  ⚠️  $dir/ 存在但为空，将从生产环境下载"
              else
                echo "  ✅ $dir/ 存在 ($FILE_COUNT 个文件)"
              fi
            fi
          done

          if [ -z "$MISSING_DIRS" ]; then
            echo "✅ 所有目录都已存在且有内容,无需下载"
            exit 0
          fi

          echo ""
          echo "📋 需要下载的目录:$MISSING_DIRS"

          # 克隆生产仓库（浅克隆,只获取最新版本）
          echo "⏬ 克隆生产仓库..."
          git clone --depth=1 --filter=blob:none --sparse \
            https://github.com/lucking7/NRRule.git \
            ./prod-temp 2>/dev/null || {
            echo "⚠️  无法克隆生产仓库,可能是首次构建"
            echo "📁 为缺失的目录创建空目录..."
            for dir in $MISSING_DIRS; do
              mkdir -p "public/$dir"
              echo "  ✅ 创建空目录: public/$dir"
            done
            exit 0
          }

          cd ./prod-temp
          # 设置要下载的所有目录
          git sparse-checkout set $MISSING_DIRS 2>/dev/null || true
          cd ..

          # 复制缺失的目录到 public
          echo ""
          echo "📂 从生产环境复制缺失的目录..."
          for dir in $MISSING_DIRS; do
            if [ -d "prod-temp/$dir" ]; then
              FILE_COUNT=$(find "prod-temp/$dir" -type f 2>/dev/null | wc -l)
              # 🔧 修复：先删除可能存在的空目录，再复制
              if [ -d "public/$dir" ]; then
                echo "  🗑️  删除已存在的空目录: public/$dir"
                rm -rf "public/$dir"
              fi
              echo "  ✅ 复制 $dir/ ($FILE_COUNT 个文件)"
              cp -r "prod-temp/$dir" "public/$dir"
            else
              echo "  ⚠️  $dir/ 在生产环境也不存在,创建空目录"
              mkdir -p "public/$dir"
            fi
          done

          # 清理临时目录
          rm -rf ./prod-temp

          echo ""
          echo "📊 最终目录状态："
          for dir in Mirror Modules Scripts List Clash sing-box Loon GeoIP; do
            if [ -d "public/$dir" ]; then
              FILE_COUNT=$(find "public/$dir" -type f 2>/dev/null | wc -l)
              echo "  ✅ $dir/: $FILE_COUNT 个文件"
            else
              echo "  ⚠️  $dir/: 不存在"
            fi
          done

          echo "✅ 目录准备完成"
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      # 🔧 步骤 5: 规则构建（始终执行）
      - run: pnpm run build
        env:
          PUBLIC_DIR: ${{ steps.build-dir.outputs.dir }}
          PROXY_BASE: https://cloudflare-proxy.lucking.workers.dev

      - name: Verify build output
        run: |
          if [ ! -d "${{ steps.build-dir.outputs.dir }}" ]; then
            echo "❌ Build directory not found"
            exit 1
          fi
          if [ ! "$(ls -A ${{ steps.build-dir.outputs.dir }})" ]; then
            echo "❌ Build directory is empty"
            exit 1
          fi
          if [ ! -f ".BUILD_FINISHED" ]; then
            echo "❌ Build finished marker not found"
            exit 1
          fi

          echo "📊 Build output statistics:"
          echo ""
          echo "📁 目录结构:"
          # 显示第一层目录结构
          ls -la public/ | head -20
          echo ""

          echo "📈 文件统计:"
          # 规则文件
          echo "  List: $(find public/List -type f 2>/dev/null | wc -l) files"
          echo "  Clash: $(find public/Clash -type f 2>/dev/null | wc -l) files"
          echo "  sing-box: $(find public/sing-box -type f 2>/dev/null | wc -l) files"
          echo "  Loon: $(find public/Loon -type f 2>/dev/null | wc -l) files"

          echo "  GeoIP: $(find public/GeoIP -type f 2>/dev/null | wc -l) files"

          # 模块和脚本
          echo ""
          echo "  Modules (all): $(find public/Modules -type f -name '*.sgmodule' 2>/dev/null | wc -l) files"
          echo "  Modules/Converted: $(find public/Modules/Converted -type f -name '*.sgmodule' 2>/dev/null | wc -l) files"
          echo "  Modules/Merged: $(find public/Modules/Merged -type f -name '*.sgmodule' 2>/dev/null | wc -l) files"
          echo "  Scripts: $(find public/Scripts -type f 2>/dev/null | wc -l) files"

          # 镜像
          echo ""
          echo "  Mirror: $(find public/Mirror -type f 2>/dev/null | wc -l) files"
          echo "    - iRingo: $(find public/Mirror/iRingo -type f 2>/dev/null | wc -l) files"
          echo "    - DualSubs: $(find public/Mirror/DualSubs -type f 2>/dev/null | wc -l) files"
          echo "    - BiliUniverse: $(find public/Mirror/BiliUniverse -type f 2>/dev/null | wc -l) files"
          echo "    - Sukka/mock: $(find public/Mirror/Sukka/mock -type f 2>/dev/null | wc -l) files"
          echo "    - Sukka/sgmodule: $(find public/Mirror/Sukka/sgmodule -type f 2>/dev/null | wc -l) files"

          # 检查 index.html 是否生成
          echo ""
          if [ -f "public/index.html" ]; then
            echo "✅ index.html generated successfully"
            echo "  Size: $(stat -f%z public/index.html 2>/dev/null || stat -c%s public/index.html 2>/dev/null || echo 'unknown') bytes"
            # 检查 index.html 中是否包含关键目录
            echo "  Checking content..."
            for dir in Mirror Modules Scripts List Clash sing-box; do
              if grep -q "$dir" public/index.html; then
                echo "    ✅ Contains $dir"
              else
                echo "    ⚠️  Missing $dir in index.html"
              fi
            done
          else
            echo "❌ index.html not found!"
          fi

          echo ""
          echo "✅ Build output verified"

      - uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: build-artifact-${{ github.sha }}-${{ github.run_number }}
          path: ${{ steps.build-dir.outputs.dir }}
          retention-days: 1
          compression-level: 4

      - name: Save cache
        if: always()
        uses: actions/cache/save@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: .cache
          key: ${{ runner.os }}-build-v3-${{ steps.date.outputs.year }}-${{ steps.date.outputs.month }}-${{ steps.date.outputs.day }}-${{ github.run_id }}



  # ============================================
  # PR 差异对比 - 预览构建变更
  # ============================================
  diff-deployment-on-pr:
    name: Diff Build Output
    needs: [prepare, build]
    if: |
      github.event_name == 'pull_request' &&
      needs.build.result == 'success'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: build-artifact-${{ github.sha }}-${{ github.run_number }}
          path: public

      - name: Compare with production
        run: |
          echo "📥 Downloading production build..."
          git clone --filter=tree:0 --no-tags \
            https://github.com/lucking7/NRRule.git ./deploy-git

          cd ./deploy-git
          echo "📊 Comparing changes..."
          rm -rf ./*
          cp -rf ../public/* ./

          echo ""
          echo "📈 File statistics:"
          git --no-pager diff --stat

          echo ""
          echo "📝 Detailed changes (first 500 lines):"
          git --no-pager diff --minimal | head -500

          echo ""
          echo "✅ Diff complete"

  # ============================================
  # 部署到 Cloudflare Pages
  # ============================================
  deploy-cloudflare:
    name: Deploy to Cloudflare Pages
    needs: [prepare, build]
    if: |
      needs.prepare.outputs.should_deploy == 'true' &&
      (needs.prepare.outputs.deploy_target == 'all' || needs.prepare.outputs.deploy_target == 'cloudflare') &&
      github.ref == 'refs/heads/main' &&
      needs.build.result == 'success'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: build-artifact-${{ github.sha }}-${{ github.run_number }}
          path: public

      # 验证构建产物
      - name: Verify build artifact
        run: |
          echo "📁 检查 Cloudflare Pages 部署内容："
          if [ ! -d "public" ]; then
            echo "❌ public 目录不存在"
            exit 1
          fi
          ls -la public/
          echo ""
          echo "📊 文件统计："
          find public -type f | wc -l
          echo "✅ 构建产物验证通过"

      # 部署到 Cloudflare Pages
      - name: Deploy to Cloudflare Pages
        id: cloudflare-deploy
        uses: cloudflare/wrangler-action@ebbaa1584979971c8614a24965b4405ff95890e0 # v4.0.0
        with:
          apiToken: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          accountId: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          command: pages deploy public --project-name=nrrule --commit-dirty=true --branch=main
          wranglerVersion: '4.114.0'
        continue-on-error: false

      # 输出部署结果
      - name: Deploy summary
        if: always()
        run: |
          if [ "${{ steps.cloudflare-deploy.outcome }}" == "success" ]; then
            echo "✅ Cloudflare Pages 部署成功"
            echo "🔗 访问地址: https://nrrule.pages.dev"
          else
            echo "❌ Cloudflare Pages 部署失败"
            echo "请检查："
            echo "  1. CLOUDFLARE_API_TOKEN 是否正确配置"
            echo "  2. CLOUDFLARE_ACCOUNT_ID 是否正确配置"
            echo "  3. 项目名称 'nrrule' 是否正确"
            exit 1
          fi

  # ============================================
  # 部署到 GitHub Repository
  # ============================================
  deploy-github:
    name: Deploy to GitHub Repository
    needs: [prepare, build]
    if: |
      needs.prepare.outputs.should_deploy == 'true' &&
      (needs.prepare.outputs.deploy_target == 'all' || needs.prepare.outputs.deploy_target == 'github') &&
      github.ref == 'refs/heads/main' &&
      needs.build.result == 'success'
    runs-on: ubuntu-latest
    steps:
      # 🔧 下载 build artifact 而不是 checkout 源代码
      - uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: build-artifact-${{ github.sha }}-${{ github.run_number }}
          path: public

      # 验证 public 目录内容
      - name: Verify public directory
        run: |
          echo "📁 Public 目录内容："
          ls -la public/ || echo "⚠️ public 目录不存在"
          echo ""
          echo "📊 文件统计："
          echo "  Modules (全部): $(find public/Modules -type f -name '*.sgmodule' 2>/dev/null | wc -l) 个文件"
          echo "  Modules/Converted: $(find public/Modules/Converted -type f -name '*.sgmodule' 2>/dev/null | wc -l) 个文件"
          echo "  Modules/Merged: $(find public/Modules/Merged -type f -name '*.sgmodule' 2>/dev/null | wc -l) 个文件"
          echo "  Scripts: $(find public/Scripts -type f 2>/dev/null | wc -l) 个文件"
          echo "  List: $(find public/List -type f 2>/dev/null | wc -l) 个文件"
          echo "  Mirror: $(find public/Mirror -type f 2>/dev/null | wc -l) 个文件"
          echo "  GeoIP: $(find public/GeoIP -type f 2>/dev/null | wc -l) 个文件"

      - name: Deploy to NRRule Repository
        run: |
          # 🔧 根据执行的任务决定要更新的目录（智能增量部署）
          SYNC_PATHS=""
          SYNC_DESC=""

          # 规则构建（始终执行）- 包含所有规则格式
          SYNC_PATHS="$SYNC_PATHS List/ Clash/ sing-box/ Loon/ GeoIP/"
          SYNC_DESC="rules"

          # 镜像同步
          if [ "${{ needs.prepare.outputs.should_mirror_sync }}" = "true" ]; then
            SYNC_PATHS="$SYNC_PATHS Mirror/"
            SYNC_DESC="$SYNC_DESC+mirrors"
          fi

          # 插件转换 / 模块合并 - 统一处理 Modules
          if [ "${{ needs.prepare.outputs.should_convert_plugins }}" = "true" ] || \
             [ "${{ needs.prepare.outputs.should_merge_modules }}" = "true" ]; then
            SYNC_PATHS="$SYNC_PATHS Modules/"
            SYNC_DESC="$SYNC_DESC+modules"
          fi

          # Scripts 目录（如果存在）
          if [ -d "public/Scripts" ]; then
            SYNC_PATHS="$SYNC_PATHS Scripts/"
            SYNC_DESC="$SYNC_DESC+scripts"
          fi

          # 去重目录列表
          SYNC_PATHS=$(echo "$SYNC_PATHS" | tr ' ' '\n' | sort -u | tr '\n' ' ')

          echo "📦 将要同步的目录（去重后）: $SYNC_PATHS"
          echo "📝 更新描述: $SYNC_DESC"

          gh repo unarchive lucking7/NRRule --yes || true

          git clone --filter=tree:0 --no-tags \
            https://${GH_USER}:${GH_TOKEN}@github.com/lucking7/NRRule.git \
            ./deploy-git

          cd ./deploy-git
          git config user.email "${GH_EMAIL}"
          git config user.name "${GH_USER}"

          # 🔧 智能增量同步：只更新有内容的目录，避免清空生产环境
          for path in $SYNC_PATHS; do
            # 检查源目录是否存在且有内容
            if [ -d "../public/$path" ]; then
              SOURCE_COUNT=$(find "../public/$path" -type f 2>/dev/null | wc -l)
              if [ "$SOURCE_COUNT" -gt 0 ]; then
                # 源目录有内容，执行同步
                if [ -d "./$path" ]; then
                  echo "🗑️  删除旧目录: $path"
                  rm -rf "./$path"
                fi
                mkdir -p "./$path"
                echo "📥 复制新内容: $path ($SOURCE_COUNT 个文件)"
                cp -rf "../public/$path"* "./$path" 2>/dev/null || true
              else
                # 源目录为空，保留生产环境现有内容
                echo "⏭️  跳过空目录: $path (保留生产环境现有内容)"
              fi
            else
              # 源目录不存在，保留生产环境现有内容
              echo "⏭️  跳过不存在的目录: $path (保留生产环境现有内容)"
            fi
          done
          
          # 复制根目录文件（如 README.md, index.html 等）
          echo ""
          echo "📄 复制根目录文件..."
          find ../public/ -maxdepth 1 -type f -exec cp {} ./ \; 2>/dev/null || true

          # 清理当前发布结构之外的顶层目录，避免增量部署保留废弃目录
          echo ""
          echo "🧹 清理当前发布结构之外的顶层目录..."
          # 有效发布根目录可能在增量构建 artifact 中缺失；同时追加当前 artifact 中的新目录。
          PUBLISHED_TOP_LEVEL_DIRS=$({
            printf '%s\n' List Clash sing-box Loon GeoIP Mirror Modules Scripts
            find ../public -mindepth 1 -maxdepth 1 -type d -exec basename {} \;
          } | sort -u | tr '\n' ' ')
          echo "  ✅ 保留目录: $PUBLISHED_TOP_LEVEL_DIRS"
          find . -mindepth 1 -maxdepth 1 -type d ! -name .git -print | while IFS= read -r dir_path; do
            dir="${dir_path#./}"
            case " $PUBLISHED_TOP_LEVEL_DIRS " in
              *" $dir "*)
                ;;
              *)
                echo "  🗑️  删除过期目录: $dir"
                rm -rf "./$dir"
                ;;
            esac
          done

          echo "📊 部署后的文件统计："
          echo "  List: $(find ./List -type f 2>/dev/null | wc -l) 个文件"
          echo "  Clash: $(find ./Clash -type f 2>/dev/null | wc -l) 个文件"
          echo "  sing-box: $(find ./sing-box -type f 2>/dev/null | wc -l) 个文件"
          echo "  Loon: $(find ./Loon -type f 2>/dev/null | wc -l) 个文件"
          echo "  Modules: $(find ./Modules -type f 2>/dev/null | wc -l) 个文件"
          echo "  Scripts: $(find ./Scripts -type f 2>/dev/null | wc -l) 个文件"
          echo "  Mirror: $(find ./Mirror -type f 2>/dev/null | wc -l) 个文件"
          echo "  GeoIP: $(find ./GeoIP -type f 2>/dev/null | wc -l) 个文件"

          git add --all .
          if git diff --cached --quiet; then
            echo "✅ 没有变更需要提交"
            exit 0
          fi
          
          git commit -m "deploy: ${{ github.repository }}@${{ github.sha }} [$SYNC_DESC]"
          git push --quiet origin HEAD:main

          cd ..
          rm -rf ./deploy-git

          gh repo archive lucking7/NRRule --yes || true
        env:
          GH_EMAIL: ${{ secrets.GIT_EMAIL }}
          GH_USER: ${{ secrets.GIT_USER }}
          GH_TOKEN: ${{ secrets.GIT_TOKEN }}
